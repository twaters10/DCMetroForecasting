# Secrets — never commit
.env
credentials/
*.pem
*credentials*.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Lambda build artifacts
package/
*.zip

# Local collector output (see LOCAL_OUTPUT_DIR)
data/local/

# ETL working data. All of it is reproducible from the S3 archive by re-running the
# pipeline, and the Parquet is far too large to track.
data/staging/
data/staging-*/
data/processed/
data/cache/
data/data_dictionary.xlsx
data/

# Trained models and their encoder mappings. Reproducible from the feature table, and
# a model binary in git is a merge conflict waiting to happen.
data/models/

# OS
.DS_Store

# Scheduled-run logs
logs/

# Notebooks
notebooks/